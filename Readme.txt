Scripts:
primative:
    prettier: runs prettier
    eslint: runs eslint
    build: build the js files
    jasmine: start jasmine
    start: starts the server
Compined:
    test: builds the project and starts jasmine
    orient: runs prettier and eslint
    go: builds the project and starts the server

End points to test
    GIT http://localhost:3000/api?name=sunflower&h=300&w=200
    parameters can be changed and new files can be added to media/original folder

Features:
    Different sizes supported
    Errors in routes and parameters are handled